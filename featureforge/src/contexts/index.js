// Contexts index file
import AuthContext, { AuthProvider, useAuth } from './AuthContext';

export {
  AuthContext,
  AuthProvider,
  useAuth
};

export default {
  AuthContext,
  AuthProvider,
  useAuth
}; 